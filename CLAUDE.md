# 开发约定

- **发版必升版本号**：每次改动打包前，package.json 的 version 加一——修问题加末位（0.6.0 → 0.6.1），加功能加中间位（0.6.x → 0.7.0），大的产品逻辑变更加首位（由产品负责人判断）。为修问题而加的能力算功能。tuner 存档记录的测试版本以此为准，版本号不升会导致不同代码对不上号
- **升号必须随当次发版提交入库**：构建不经过 git，只改 package.json 不提交，事后就查不到某个已发版本对应的代码（1.8.x 曾因此在 git 里无迹可查）
- **发版提交后随手推 GitHub**（`git push origin master`）：不推会一直积在本地，2026-08-31 发现积压了 36 个提交
- 打包命令 `pnpm build:mac`；无签名证书，首次打开需右键选"打开"
